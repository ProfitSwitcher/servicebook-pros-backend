# ServiceBook Pros Backend Environment Variables
# 
# For Railway deployment, set these as environment variables in your Railway dashboard:
# https://railway.app/dashboard/[your-project]/variables
#
# Required database configuration (Railway will provide DATABASE_URL automatically if you add a PostgreSQL service)
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Alternative individual database settings (used if DATABASE_URL is not provided)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=servicebook

# Required JWT secret for authentication (generate a secure random string)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# Optional: Node environment (Railway sets this automatically)
NODE_ENV=production

# Optional: Port (Railway sets this automatically)
PORT=3000